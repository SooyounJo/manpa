# Normalize text files in repo to LF (Git will handle CRLF on checkout via core.autocrlf)
* text=auto eol=lf

# Keep binaries untouched
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.svg -text


